#!/bin/bash

tmux list-sessions -F '#{?session_attached,,#{session_name}}' |
  fzf --reverse -m --header=kill-session \
    --preview 'tmux capture-pane -pt {} | bat --color=always' |
  xargs -I {} tmux kill-session -t {}
